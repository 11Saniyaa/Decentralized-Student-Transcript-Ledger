{
  "name": "student-transcript-ledger",
  "version": "1.0.0",
  "description": "Decentralized student transcript management on blockchain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "setup:local": "hardhat run scripts/setup.js --network localhost",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "demo": "hardhat run scripts/demo.js --network localhost",
    "verify:local": "hardhat run scripts/verify.js --network localhost",
    "flatten": "hardhat flatten contracts/StudentTranscriptLedger.sol > flattened.sol"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "smart-contracts",
    "education",
    "credentials",
    "transcript",
    "decentralized"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "chai": "^4.3.8",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0",
    "mocha": "^10.2.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "ethers": "^6.15.0",
    "ipfs-http-client": "^60.0.1",
    "web3.storage": "^4.5.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
